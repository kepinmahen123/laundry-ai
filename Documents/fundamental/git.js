// push === upload code dari lokal ke github
// pull === download code dari github ke lokal
// origin === var yg menyimpan url github
// git remote set-url origin urlbaru === mengganti url di dalam origin
// origin remote show origin === melihat isi dari origin (melihat url tujuan push/pull)
// 